{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "providers": ["node"],
      "phases": {
        "install": {
          "cmds": ["cd backend && npm install"]
        },
        "start": {
          "cmds": ["cd backend && npm start"]
        }
      }
    }
  }
}
